venv/
__pycache__/
*.pyc

# 実機ごとのキャリブレーション値・ホームポジション
# (calibrate_homing.pyで生成)
servo_calibration.json
home_position.json

# C++版IK拡張のビルド成果物 (build_cpp_ik.ps1で生成、環境依存)
cpp_ik/build/
cpp_ik/*.pyd
cpp_ik/*.so
